# Local media assets

The video and audio source files used by this Remotion project are intentionally
excluded from Git because several files exceed GitHub's regular file-size limit.

Expected local asset locations include:

- `public/`
- `Opus/`
- media files in the project root

Copy the original media files into those locations before previewing or rendering
compositions that depend on them.
